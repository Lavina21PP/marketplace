generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Users1 {
  u_id    Int    @id @default(autoincrement())
  u_name  String
  u_email String @unique
}